# Start with GitHub Pages project paths

The first deployment assumes a GitHub Pages project site and configures its repository path prefix. A custom domain can be added later without changing the Catalog or author repository model.
