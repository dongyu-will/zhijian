# Publish only static Astro output

The production site publishes Astro's static `dist/` directory to GitHub Pages. V1 has no runtime API, database, server-side rendering process, or browser-time fetch of author repositories.
